import 'package:flutter/material.dart';

class SetDivider {
  static Divider thin = const Divider(
    thickness: 1,
    height: 1,
  );

  static Divider thick = const Divider(
    thickness: 2,
    height: 2,
  );
}
